1. 新增语义化标签 header section footer aside article
2. 新增多媒体标签 audio, video
3. 增强型表单form 属性及元素 datalist progress meter刻度尺 required minlength autofocus
4. canvas画布 使用 JavaScript 在就可以网页上绘制图像；
5. 拖放API  draggable属性设置元素可拖放
6. SVG绘图
8. Web Worker 允许一段JavaScript程序运行在主线程之外的另外一个线程中,分别是专用线程Dedicated Worker和共享线程 Shared Worker
9. Web Storage  Session Local
10. Web Socket 协议，允许服务器端向客户端发送数据，是一种全双工（允许数据在两个方向上同时传输）、双向的通信方式
可以双向发送或接受信息 HTTP是单向的 WebSocket是需要浏览器和服务器握手进行建立连接的